{
  "name": "focustime",
  "version": "2.0.0",
  "description": "Block distracting sites",
  "license": "MIT",
  "scripts": {
    "start": "npm run build && web-ext run",
    "build": "rm -rf dist/ && webpack --mode development",
    "ext-build": "npm run build && web-ext build",
    "lint": "web-ext lint"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thesoenke/focustime.git"
  },
  "devDependencies": {
    "ts-loader": "^5.3.3",
    "typescript": "^3.3.1",
    "web-ext": "^2.9.3",
    "web-ext-types": "^3.1.0",
    "webpack": "^4.29.0",
    "webpack-cli": "^3.2.1"
  }
}
